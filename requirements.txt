# Python dependencies for the zotero-split-scans skill.
# Versions below are what the skill was developed and tested against;
# the >= pins are the practical minimums.
#
# Note: scripts/zsplit.py contains a small runtime monkeypatch tuned to
# pyzotero 1.7.6's attachment-upload path (httpx write-timeout + a removed
# httpx.ConnectionError alias). If you upgrade pyzotero substantially, re-check
# that patch in get_client()/the module header.

PyMuPDF>=1.26.7    # imported as `fitz` — extracts the embedded scan photo per page
pyzotero>=1.7.6    # Zotero Web API client (item create, attachment upload, notes)
pypdf>=6.6.2       # splits the source PDF while preserving the OCR text layer
httpx>=0.26.0      # transitive (pyzotero uses it); patched directly by the script
